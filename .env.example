# ADK Companion 环境变量示例
# 复制此文件为 .env 并填入真实值
# 注意：.env 包含敏感信息，切勿提交到 Git

# ========================================
# Google Cloud / Vertex AI 配置
# ========================================
GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# ========================================
# GitHub API Token 配置
# ========================================

# GitHub Token（主智能体使用）
# 用于检查上游 release、创建 PR 等常规操作
# 在 GitHub Settings → Developer settings → Personal access tokens 生成
GITHUB_TOKEN=ghp_your_main_token_here

# PR 审查智能体专用 GitHub Token
# 用于审查 PR、合并 PR、请求审查等操作
# 建议使用不同用户的 Token 以确保审查独立性和客观性
REVIEW_GITHUB_TOKEN=ghp_your_review_token_here

# 可选：上游仓库（默认为 google/adk-python）
UPSTREAM_REPO=google/adk-python

# ========================================
# Token 权限说明
# ========================================

# GITHUB_TOKEN 权限建议：
# - repo (完整仓库访问权限)
# - read:org (读取组织信息，如需要)
# - read:user (读取用户信息)

# REVIEW_GITHUB_TOKEN 权限建议：
# - repo (完整仓库访问权限)
# - pull_request:write (创建和管理 PR 审查)
# - read:org (读取组织信息，如需要)
# - read:user (读取用户信息)

# ========================================
# 安全建议
# ========================================
# 1. 使用不同 GitHub 账户的 Token
# 2. 定期轮换 Token（建议每3-6个月）
# 3. 使用最小权限原则
# 4. 不要在代码中硬编码 Token

# ========================================
# CI/CD 专用（在 GitHub Secrets 中配置，不要放在本地 .env）
# ========================================
# GCP_CREDENTIALS={"type":"service_account","project_id":"..."}
# PAT_TOKEN=ghp_xxx